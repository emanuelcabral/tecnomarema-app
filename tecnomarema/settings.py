
"""
Django settings for tecnomarema.

Generated by 'django-admin startproject' using Django 5.2.
"""

from pathlib import Path
import os

# BASE_DIR es la raíz del proyecto
BASE_DIR = Path(__file__).resolve().parent.parent

# Seguridad
SECRET_KEY = 'django-insecure-y_9!qw9gm#r@)#v!liba3@37%1pjyldyad2ae-vwxd$!%kqcy@'
DEBUG = True
ALLOWED_HOSTS = []

# Aplicaciones instaladas
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'widget_tweaks', # django-widget-tweaks
    'plataforma',  # mi app personalizada
]

# Middlewares
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'tecnomarema.urls'

# Templates
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'plataforma' / 'templates'],  # Ruta a tus plantillas
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',  # importante para auth
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'plataforma.context_processors.nombre_usuario',  #"agregado para el usuario en el contexto global"
            ],
        },
    },
]

WSGI_APPLICATION = 'tecnomarema.wsgi.application'

# Base de datos
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'tecnomarema',
        'USER': 'postgres',
        'PASSWORD': '9560',  # poné la contraseña que usaste al instalar PostgreSQL
        'HOST': 'localhost',
        'PORT': '5432',
    }
}

# Validación de contraseñas
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]

# Internacionalización
LANGUAGE_CODE = 'es'
TIME_ZONE = 'UTC'
USE_I18N = True
USE_TZ = True

# Archivos estáticos (CSS, JS, imágenes)
STATIC_URL = 'static/'

# ✅ Ruta donde Django buscará archivos estáticos durante el desarrollo
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'plataforma', 'static'),
]

# Clave por defecto para campos ID
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Configuración de email para recuperación de contraseña
# Por ahora usamos consola para pruebas: los mails se muestran en la consola
# EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'

# Si querés usar Gmail o SMTP real, reemplazá por esta configuración y poné tus datos reales:
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'tecnomarema.ar@gmail.com'
EMAIL_HOST_PASSWORD = 'crzhdkntscrvdabg'
# DEFAULT_FROM_EMAIL = 'noreply@tecnomarema.com' asi deberia estar despues se debe configurar
DEFAULT_FROM_EMAIL = 'tecnomarema.ar@gmail.com'

# Redirecciones login/logout

# AUTH_USER_MODEL = 'plataforma.Usuario'
AUTH_USER_MODEL = 'plataforma.PerfilUsuario'
LOGIN_URL = 'login'
LOGIN_REDIRECT_URL = 'mis_cursos'
LOGOUT_REDIRECT_URL = 'login'

#para subir imagenes
# MEDIA_URL = '/media/'
# MEDIA_ROOT = os.path.join(BASE_DIR, 'media')


MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'


